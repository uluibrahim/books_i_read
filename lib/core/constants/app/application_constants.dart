import 'dart:ui';

class ApplicationConstants {
  static const appName = 'Kitaplarım';
  static const langAssetPath = 'assets/language';
  static const appWebSite = '';
  static const email = "";

  static const trLocale = Locale("tr", "TR");
  static const supportedLocale = [ApplicationConstants.trLocale];
}
